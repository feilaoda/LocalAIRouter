{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "Allow the main LocalAIRouter window to access desktop IPC commands.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
